# Workflow Status - BMad Method
# Este arquivo rastreia o progresso através das fases de Análise, Planejamento e Solutioning do BMM.
# A fase de Implementação é rastreada separadamente em sprint-status.yaml

# DEFINIÇÕES DE STATUS:
# ==================
# Status Inicial (antes da conclusão):
#   - required: Deve ser concluído para progredir
#   - optional: Pode ser concluído mas não é obrigatório
#   - recommended: Fortemente sugerido mas não obrigatório
#   - conditional: Obrigatório apenas se certas condições forem atendidas (ex: if_has_ui)
#
# Status de Conclusão:
#   - {file-path}: Arquivo criado/encontrado (ex: "docs/product-brief.md")
#   - skipped: Workflow opcional/condicional que foi pulado

generated: "2025-12-01"
project: "star-tickets"
project_description: "Sistema de senhas para atendimento em estabelecimentos comerciais com backend Node.js + SQLite e frontend web"
selected_track: "method"
field_type: "greenfield"
workflow_path: ".bmad/bmm/workflows/workflow-status/paths/method-greenfield.yaml"

workflow_status:
  # ============================================
  # FASE 0 - Discovery (Opcional)
  # ============================================
  phase_0_discovery:
    brainstorm-project: "required"  # Selecionado pelo usuário
    research: "skipped"  # Não selecionado
    product-brief: "skipped"  # Não selecionado

  # ============================================
  # FASE 1 - Planning
  # ============================================
  phase_1_planning:
    prd: "required"
    validate-prd: "optional"
    create-ux-design: "conditional"  # Se tiver UI (star-tickets TEM UI)

  # ============================================
  # FASE 2 - Solutioning
  # ============================================
  phase_2_solutioning:
    create-architecture: "required"
    create-epics-and-stories: "required"
    test-design: "recommended"
    validate-architecture: "optional"
    implementation-readiness: "required"  # Gate check final

  # ============================================
  # FASE 3 - Implementation
  # ============================================
  phase_3_implementation:
    sprint-planning: "required"
    # Após sprint-planning, o rastreamento continua em sprint-status.yaml

# Notas do projeto
notes:
  - "Projeto greenfield - novo sistema de senhas"
  - "Stack: Node.js + SQLite + Frontend Web"
  - "Sistema configurável via tabelas com menus dinâmicos"
  - "Iniciado com workflow brainstorm-project"
